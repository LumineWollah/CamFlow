# Basic Rig (Sample)

This sample is a minimal setup guide.

## Setup
- Add `CameraInputProvider`, `CameraMotor`, and `CameraController` on your Camera.
- Optional: add `CameraTargetFollower` and assign a target.
- Optional: add `CameraBounds` and set a center/size AABB.
