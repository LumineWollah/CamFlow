# CamFlow – Demo Scene

This sample demonstrates all every feature of **CamFlow**.

## Requirements

This sample requires the following Unity packages:

- com.unity.spline

## Setup

Before opening the scene:
1. Open **Window → Package Manager**
2. Install the required packages
3. Import this sample via the Package Manager

If dependencies are missing, the scene may show errors.
